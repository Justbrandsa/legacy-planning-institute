import {NextRequest,NextResponse} from 'next/server';export async function POST(req:NextRequest){try{const lead=await req.json();if(!lead.email||!lead.phone)return NextResponse.json({error:'Missing required fields'},{status:400});console.info('Lead received',{...lead,receivedAt:new Date().toISOString()});return NextResponse.json({ok:true});}catch{return NextResponse.json({error:'Invalid request'},{status:400})}}
